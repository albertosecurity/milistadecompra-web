# Yepar Check — Web PWA

App web para gestión de listas de compras conectada a Railway backend.

## Deploy en Railway

1. Conectar este repo a Railway
2. Railway detecta el `railway.toml` y hace build automático
3. La app queda disponible en la URL pública de Railway
